<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiangmu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/touxiang_cf"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/touxiang_cf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".MyInfosActivity" />
        <activity android:name=".MyGroupActivity" />
        <activity android:name=".CityListActivity" />
        <activity android:name=".TJDDActivity" />
        <activity android:name=".AddAddrActivity" />
        <activity android:name=".MainFootActivity" />
        <activity android:name=".CityAddActivity" />
        <activity android:name=".MyWalletActivity"></activity>
    </application>
</manifest>